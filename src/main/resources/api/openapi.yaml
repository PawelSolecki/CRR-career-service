openapi: 3.0.3
info:
  title: My API
  version: 0.0.1
servers:
  - url: http://localhost:8081
paths:
  /api/v1/scrapper:
    $ref: "paths/scrapper/scrape.yaml#/scrape"
  /api/v1/pdf/generate:
     $ref: "paths/pdf/generate.yaml#/generate"
